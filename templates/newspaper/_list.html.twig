<table>
    <thead>
        <tr>
            <th>Newspapers</th>
        </tr>
    </thead>
    <tbody>
        {% for newspaper in newspapers %}
        <tr>
            <td> <a href="{{ path('app_newspaper_show', {id: newspaper.id}) }}">{{ newspaper.name }}</a></td>
        </tr>
        {% endfor %}
    </tbody>
</table>